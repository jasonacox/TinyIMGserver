# Apple Silicon optimized requirements for TinyIMGserver
# This file contains compatible versions for macOS with Apple Silicon

# Core ML frameworks - use CPU/MPS optimized versions
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Diffusers and transformers - latest stable versions
diffusers>=0.24.0
transformers>=4.35.0
accelerate>=0.25.0

# Hugging Face hub - latest
huggingface_hub>=0.19.0

# Image processing
Pillow>=10.0.0
safetensors>=0.4.0

# Optional but recommended for Apple Silicon
# peft>=0.7.0  # Commented out as it's causing issues

# FastAPI and web dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
